name: Rust CI

on:
  push:
    branches: [master, main, "dev/**"]
    paths-ignore:
      - "**.md"
      - "docs/**"
  pull_request:
    branches: [master, main]

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Check & Lint
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Install system dependencies (ubuntu)
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get update && sudo apt-get install -y libwayland-dev libxkbcommon-dev libegl1-mesa-dev libpipewire-0.3-dev libclang-dev libssl-dev libavcodec-dev libavutil-dev libavformat-dev libavfilter-dev libavdevice-dev libswresample-dev libswscale-dev libpostproc-dev

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2

      - name: Check (default features)
        run: cargo check --all-targets

      - name: Check (ffmpeg feature)
        if: matrix.os == 'ubuntu-latest'
        run: cargo check --features ffmpeg

      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings

      - name: Format check
        run: cargo fmt --all --check

  build:
    name: Build
    needs: check
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install system dependencies (ubuntu)
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get update && sudo apt-get install -y libwayland-dev libxkbcommon-dev libegl1-mesa-dev libpipewire-0.3-dev libclang-dev libssl-dev libavcodec-dev libavutil-dev libavformat-dev libavfilter-dev libavdevice-dev libswresample-dev libswscale-dev libpostproc-dev


      - name: Rust Cache
        uses: Swatinem/rust-cache@v2

      - name: Build binary
        shell: bash
        run: cargo build --release >build.log 2>&1

      - name: Show build errors
        if: failure() && hashFiles('build.log') != ''
        shell: bash
        run: |
          echo "::error::build log has $(wc -l < build.log) lines"
          # Extract linker/compiler errors
          grep -ni 'error:' build.log | tail -10 | while IFS= read -r line; do
            echo "::error::build: $line"
          done
          grep -ni '= note:' build.log | tail -20 | while IFS= read -r line; do
            echo "::error::build-note: $line"
          done
          # Full log in step summary
          echo "### Build output" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          tail -200 build.log >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: Upload build error log
        if: failure() && hashFiles('build.log') != ''
        uses: actions/upload-artifact@v4
        with:
          name: build-error-log-${{ matrix.os }}
          path: build.log

      - name: Upload binary artifacts
        uses: actions/upload-artifact@v4
        with:
          name: chronodesk-bin-${{ matrix.os }}
          path: |
            target/release/chronodesk${{ matrix.os == 'windows-latest' && '.exe' || '' }}
            target/release/signaling-server${{ matrix.os == 'windows-latest' && '.exe' || '' }}
          if-no-files-found: warn

      - name: Upload library artifacts
        uses: actions/upload-artifact@v4
        with:
          name: chronodesk-lib-${{ matrix.os }}
          path: |
            target/release/libchronodesk.so
            target/release/libchronodesk.dylib
            target/release/chronodesk.dll
          if-no-files-found: warn

  test:
    name: Test
    needs: check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install system dependencies (ubuntu)
        run: sudo apt-get update && sudo apt-get install -y libwayland-dev libxkbcommon-dev libegl1-mesa-dev libpipewire-0.3-dev libclang-dev libssl-dev libavcodec-dev libavutil-dev libavformat-dev libavfilter-dev libavdevice-dev libswresample-dev libswscale-dev libpostproc-dev

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2

      - name: Run tests
        shell: bash
        run: cargo test --all-targets >test-output.log 2>&1

      - name: Show test errors
        if: failure()
        shell: bash
        run: |
          echo "::error::test log has $(wc -l < test-output.log) lines"
          grep -ni 'error:\|FAILED\|panicked at\|thread ' test-output.log | tail -20 | while IFS= read -r line; do
            echo "::error::test: $line"
          done
          grep -ni '= note:' test-output.log | tail -20 | while IFS= read -r line; do
            echo "::error::test-note: $line"
          done
          echo "### Test output" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          tail -200 test-output.log >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: Upload test error log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-error-log
          path: test-output.log

  flutter:
    name: Flutter Analyze
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Enable Linux desktop
        run: flutter config --enable-linux-desktop

      - name: Flutter pub get
        run: cd chronodesk_flutter && flutter pub get

      - name: Flutter analyze
        run: cd chronodesk_flutter && flutter analyze --no-fatal-infos --no-fatal-warnings
